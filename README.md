# AsciiCam
Small script to convert a webcam photo or an image file to ASCII art.

After converting, you can choose to open an already formatted version on a local html file, in the /webphoto folder. This is done automatically if chosen.
